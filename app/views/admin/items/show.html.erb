<h4>商品詳細</h4>

<%= image_tag @item.get_image(200, 200) %>

<p>商品名 <%= @item.name %></p>
<p>商品説明 <%= @item.description %></p>
<p>ジャンル <%= @item.genre.name %></p>
<p>税込み価格<br>(税抜価格) <%= (@item.price * 1.1).floor %>(<%= @item.price %>) 円</p>
<p>販売ステータス
  <% if @item.sales_status %>
    販売中
  <% else %>
    販売停止中
  <% end %>
</p>

<p><%= link_to "編集する", edit_admin_item_path(@item) %></p>