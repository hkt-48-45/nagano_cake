<div class="container">
  <div class="row justify-content-md-center">
    <div class="col col-lg-2">

    </div>
    <div class="col-md-auto text-center">
      <h4 class="center">商品詳細</h4>
    <%= image_tag @item.get_image(400, 400) %>
    </div>
    <div class="col col-lg-4">

    </div>
  </div>
  <div class="row justify-content-md-center">
    <div class="col-md-auto text-left">
      <h4>商品名: <%= @item.name %></h4>
      <h4>商品説明: <%= @item.description %></h4>
      <h4>ジャンル: <%= @item.genre.name %></h4>
      <h4>税込価格(税抜価格):<%= (@item.price * 1.1).floor %>円(<%= @item.price %>円)</h4>
      <h4>販売ステータス:
        <% if @item.sales_status %>
          販売中
        <% else %>
          販売停止中
        <% end %>
      </h4>

      <p><%= link_to "編集する", edit_admin_item_path(@item), class: "btn btn-sm btn-success" %></p>
    </div>
    <div class="col col-lg-2">

    </div>
  </div>
</div>