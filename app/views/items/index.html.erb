<%= render "genre_search", genres: @genres %>

<h4>商品一覧(全<%= @items_all.count %>件)</h4>

<% @items.each do |item| %>
  <%= link_to item_path(item) do %>
    <%= image_tag item.get_image(100, 100) %><br>
  <% end %>
  <%= item.name %><br>
  ¥<%= item.price %><br>
<% end %><%= render "items/genre_search", genres: @genres %>
<% end %>

<%= paginate @items %>
